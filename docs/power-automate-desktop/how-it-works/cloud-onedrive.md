# OneDrive (cloud) — how each function works

Default-pane **OneDrive (cloud)** operations. Requires a connection reference. File payloads are binary data.

25 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Add file tag

- **Id:** `onedrive/add-file-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Adds file tag.

**Use case.** In personal OneDrive files, drop **Add file tag** on the canvas. Adds file tag.

**Demonstration.**

```text
**Add file tag**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Convert file

- **Id:** `onedrive/convert-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Converts file.

**Use case.** In personal OneDrive files, drop **Convert file** on the canvas. Converts file.

**Demonstration.**

```text
**Convert file**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Convert file using path

- **Id:** `onedrive/convert-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Converts file using path.

**Use case.** In personal OneDrive files, drop **Convert file using path** on the canvas. Converts file using path.

**Demonstration.**

```text
**Convert file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Copy file

- **Id:** `onedrive/copy-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Copies file.

**Use case.** In personal OneDrive files, drop **Copy file** on the canvas. Copies file.

**Demonstration.**

```text
**Copy file**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Copy file using path

- **Id:** `onedrive/copy-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Copies file using path.

**Use case.** In personal OneDrive files, drop **Copy file using path** on the canvas. Copies file using path.

**Demonstration.**

```text
**Copy file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Create file

- **Id:** `onedrive/create-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates file.

**Use case.** In personal OneDrive files, drop **Create file** on the canvas. Creates file.

**Demonstration.**

```text
**Create file**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Create share link

- **Id:** `onedrive/create-share-link`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates share link.

**Use case.** In personal OneDrive files, drop **Create share link** on the canvas. Creates share link.

**Demonstration.**

```text
**Create share link**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Create share link by path

- **Id:** `onedrive/create-share-link-by-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates share link by path.

**Use case.** In personal OneDrive files, drop **Create share link by path** on the canvas. Creates share link by path.

**Demonstration.**

```text
**Create share link by path**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Delete file

- **Id:** `onedrive/delete-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes file.

**Use case.** In personal OneDrive files, drop **Delete file** on the canvas. Deletes file.

**Demonstration.**

```text
**Delete file**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Extract archive to folder

- **Id:** `onedrive/extract-archive-to-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Extracts archive to folder.

**Use case.** In personal OneDrive files, drop **Extract archive to folder** on the canvas. Extracts archive to folder.

**Demonstration.**

```text
**Extract archive to folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Find files in folder

- **Id:** `onedrive/find-files-in-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Finds files in folder.

**Use case.** In personal OneDrive files, drop **Find files in folder** on the canvas. Finds files in folder.

**Demonstration.**

```text
**Find files in folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Find files in folder by path

- **Id:** `onedrive/find-files-in-folder-by-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Finds files in folder by path.

**Use case.** In personal OneDrive files, drop **Find files in folder by path** on the canvas. Finds files in folder by path.

**Demonstration.**

```text
**Find files in folder by path**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file content

- **Id:** `onedrive/get-file-content`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file content into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file content** on the canvas. Reads file content into a flow variable.

**Demonstration.**

```text
**Get file content**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file content using path

- **Id:** `onedrive/get-file-content-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file content using path into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file content using path** on the canvas. Reads file content using path into a flow variable.

**Demonstration.**

```text
**Get file content using path**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file metadata

- **Id:** `onedrive/get-file-metadata`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file metadata into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file metadata** on the canvas. Reads file metadata into a flow variable.

**Demonstration.**

```text
**Get file metadata**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file metadata using path

- **Id:** `onedrive/get-file-metadata-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file metadata using path into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file metadata using path** on the canvas. Reads file metadata using path into a flow variable.

**Demonstration.**

```text
**Get file metadata using path**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file tags

- **Id:** `onedrive/get-file-tags`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file tags into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file tags** on the canvas. Reads file tags into a flow variable.

**Demonstration.**

```text
**Get file tags**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Get file thumbnail

- **Id:** `onedrive/get-file-thumbnail`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file thumbnail into a flow variable.

**Use case.** In personal OneDrive files, drop **Get file thumbnail** on the canvas. Reads file thumbnail into a flow variable.

**Demonstration.**

```text
**Get file thumbnail**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### List files in folder

- **Id:** `onedrive/list-files-in-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists files in folder.

**Use case.** In personal OneDrive files, drop **List files in folder** on the canvas. Lists files in folder.

**Demonstration.**

```text
**List files in folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### List files in root folder

- **Id:** `onedrive/list-files-in-root-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists files in root folder.

**Use case.** In personal OneDrive files, drop **List files in root folder** on the canvas. Lists files in root folder.

**Demonstration.**

```text
**List files in root folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Move or rename a file

- **Id:** `onedrive/move-or-rename-a-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Moves or rename a file.

**Use case.** In personal OneDrive files, drop **Move or rename a file** on the canvas. Moves or rename a file.

**Demonstration.**

```text
**Move or rename a file**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Move or rename a file using path

- **Id:** `onedrive/move-or-rename-a-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Moves or rename a file using path.

**Use case.** In personal OneDrive files, drop **Move or rename a file using path** on the canvas. Moves or rename a file using path.

**Demonstration.**

```text
**Move or rename a file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Remove file tag

- **Id:** `onedrive/remove-file-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Removes file tag.

**Use case.** In personal OneDrive files, drop **Remove file tag** on the canvas. Removes file tag.

**Demonstration.**

```text
**Remove file tag**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Update file

- **Id:** `onedrive/update-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates file.

**Use case.** In personal OneDrive files, drop **Update file** on the canvas. Updates file.

**Demonstration.**

```text
**Update file**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.

### Upload file from URL

- **Id:** `onedrive/upload-file-from-url`
- **Kind:** cloud-connector-operation
- **Purpose:** Uploads file from URL.

**Use case.** In personal OneDrive files, drop **Upload file from URL** on the canvas. Uploads file from URL.

**Demonstration.**

```text
**Upload file from URL**
- (no inputs)
```

**Analogy.** One tool in that kit: your own locker at work.

**In combination.** Get file content using path, convert binary to a local file, process, upload back.
