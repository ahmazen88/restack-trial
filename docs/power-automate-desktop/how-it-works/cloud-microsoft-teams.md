# Microsoft Teams (cloud) — how each function works

Default-pane **Microsoft Teams (cloud)** operations. Requires a connection reference. File payloads are binary data.

27 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Add a member to a tag

- **Id:** `microsoft-teams/add-a-member-to-a-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Adds a member to a tag.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Add a member to a tag** on the canvas. Adds a member to a tag.

**Demonstration.**

```text
**Add a member to a tag**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Add a member to a team

- **Id:** `microsoft-teams/add-a-member-to-a-team`
- **Kind:** cloud-connector-operation
- **Purpose:** Adds a member to a team.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Add a member to a team** on the canvas. Adds a member to a team.

**Demonstration.**

```text
**Add a member to a team**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Create a channel

- **Id:** `microsoft-teams/create-a-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a channel.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Create a channel** on the canvas. Creates a channel.

**Demonstration.**

```text
**Create a channel**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Create a chat

- **Id:** `microsoft-teams/create-a-chat`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a chat.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Create a chat** on the canvas. Creates a chat.

**Demonstration.**

```text
**Create a chat**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Create a tag for a team

- **Id:** `microsoft-teams/create-a-tag-for-a-team`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a tag for a team.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Create a tag for a team** on the canvas. Creates a tag for a team.

**Demonstration.**

```text
**Create a tag for a team**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Create a team

- **Id:** `microsoft-teams/create-a-team`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a team.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Create a team** on the canvas. Creates a team.

**Demonstration.**

```text
**Create a team**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Create a Teams meeting

- **Id:** `microsoft-teams/create-a-teams-meeting`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a Teams meeting.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Create a Teams meeting** on the canvas. Creates a Teams meeting.

**Demonstration.**

```text
**Create a Teams meeting**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Delete a member from a tag

- **Id:** `microsoft-teams/delete-a-member-from-a-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes a member from a tag.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Delete a member from a tag** on the canvas. Deletes a member from a tag.

**Demonstration.**

```text
**Delete a member from a tag**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Delete a tag

- **Id:** `microsoft-teams/delete-a-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes a tag.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Delete a tag** on the canvas. Deletes a tag.

**Demonstration.**

```text
**Delete a tag**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Get a team

- **Id:** `microsoft-teams/get-a-team`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads a team into a flow variable.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Get a team** on the canvas. Reads a team into a flow variable.

**Demonstration.**

```text
**Get a team**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Get an @mention token for a tag

- **Id:** `microsoft-teams/get-an-mention-token-for-a-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads an @mention token for a tag into a flow variable.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Get an @mention token for a tag** on the canvas. Reads an @mention token for a tag into a flow variable.

**Demonstration.**

```text
**Get an @mention token for a tag**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Get an @mention token for a user

- **Id:** `microsoft-teams/get-an-mention-token-for-a-user`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads an @mention token for a user into a flow variable.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Get an @mention token for a user** on the canvas. Reads an @mention token for a user into a flow variable.

**Demonstration.**

```text
**Get an @mention token for a user**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Get message details

- **Id:** `microsoft-teams/get-message-details`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads message details into a flow variable.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Get message details** on the canvas. Reads message details into a flow variable.

**Demonstration.**

```text
**Get message details**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Get messages

- **Id:** `microsoft-teams/get-messages`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads messages into a flow variable.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Get messages** on the canvas. Reads messages into a flow variable.

**Demonstration.**

```text
**Get messages**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List all tags for a team

- **Id:** `microsoft-teams/list-all-tags-for-a-team`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists all tags for a team.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List all tags for a team** on the canvas. Lists all tags for a team.

**Demonstration.**

```text
**List all tags for a team**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List channels

- **Id:** `microsoft-teams/list-channels`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists channels.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List channels** on the canvas. Lists channels.

**Demonstration.**

```text
**List channels**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List chats

- **Id:** `microsoft-teams/list-chats`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists chats.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List chats** on the canvas. Lists chats.

**Demonstration.**

```text
**List chats**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List members

- **Id:** `microsoft-teams/list-members`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists members.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List members** on the canvas. Lists members.

**Demonstration.**

```text
**List members**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List teams

- **Id:** `microsoft-teams/list-teams`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists teams.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List teams** on the canvas. Lists teams.

**Demonstration.**

```text
**List teams**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### List the members for a tag

- **Id:** `microsoft-teams/list-the-members-for-a-tag`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists the members for a tag.

**Use case.** In notifying a channel when a bot finishes or fails, drop **List the members for a tag** on the canvas. Lists the members for a tag.

**Demonstration.**

```text
**List the members for a tag**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Post a feed notification

- **Id:** `microsoft-teams/post-a-feed-notification`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Post a feed notification** from the actions pane.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Post a feed notification** on the canvas. Runs **Post a feed notification** from the actions pane.

**Demonstration.**

```text
**Post a feed notification**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Post card in a chat or channel

- **Id:** `microsoft-teams/post-card-in-a-chat-or-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Post card in a chat or channel** from the actions pane.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Post card in a chat or channel** on the canvas. Runs **Post card in a chat or channel** from the actions pane.

**Demonstration.**

```text
**Post card in a chat or channel**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Post message in a chat or channel

- **Id:** `microsoft-teams/post-message-in-a-chat-or-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Post message in a chat or channel** from the actions pane.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Post message in a chat or channel** on the canvas. Runs **Post message in a chat or channel** from the actions pane.

**Demonstration.**

```text
**Post message in a chat or channel**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Reply with a message in a channel

- **Id:** `microsoft-teams/reply-with-a-message-in-a-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Reply with a message in a channel** from the actions pane.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Reply with a message in a channel** on the canvas. Runs **Reply with a message in a channel** from the actions pane.

**Demonstration.**

```text
**Reply with a message in a channel**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Reply with adaptive card in a channel

- **Id:** `microsoft-teams/reply-with-adaptive-card-in-a-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Reply with adaptive card in a channel** from the actions pane.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Reply with adaptive card in a channel** on the canvas. Runs **Reply with adaptive card in a channel** from the actions pane.

**Demonstration.**

```text
**Reply with adaptive card in a channel**
- (no inputs)
```

**Analogy.** One tool in that kit: leaning into the team room and saying the job is done.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Send a Microsoft Graph HTTP request

- **Id:** `microsoft-teams/send-a-microsoft-graph-http-request`
- **Kind:** cloud-connector-operation
- **Purpose:** Sends a Microsoft Graph HTTP request.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Send a Microsoft Graph HTTP request** on the canvas. Sends a Microsoft Graph HTTP request.

**Demonstration.**

```text
**Send a Microsoft Graph HTTP request**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Post message in a chat or channel after Stop flow or On block error.

### Update an adaptive card in a chat or channel

- **Id:** `microsoft-teams/update-an-adaptive-card-in-a-chat-or-channel`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates an adaptive card in a chat or channel.

**Use case.** In notifying a channel when a bot finishes or fails, drop **Update an adaptive card in a chat or channel** on the canvas. Updates an adaptive card in a chat or channel.

**Demonstration.**

```text
**Update an adaptive card in a chat or channel**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Post message in a chat or channel after Stop flow or On block error.
