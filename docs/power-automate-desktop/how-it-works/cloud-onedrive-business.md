# OneDrive for work or school (cloud) — how each function works

Default-pane **OneDrive for work or school (cloud)** operations. Requires a connection reference. File payloads are binary data.

22 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Convert file

- **Id:** `onedrive-business/convert-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Converts file.

**Use case.** In work OneDrive files, drop **Convert file** on the canvas. Converts file.

**Demonstration.**

```text
**Convert file**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Convert file using path

- **Id:** `onedrive-business/convert-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Converts file using path.

**Use case.** In work OneDrive files, drop **Convert file using path** on the canvas. Converts file using path.

**Demonstration.**

```text
**Convert file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Copy file

- **Id:** `onedrive-business/copy-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Copies file.

**Use case.** In work OneDrive files, drop **Copy file** on the canvas. Copies file.

**Demonstration.**

```text
**Copy file**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Copy file using path

- **Id:** `onedrive-business/copy-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Copies file using path.

**Use case.** In work OneDrive files, drop **Copy file using path** on the canvas. Copies file using path.

**Demonstration.**

```text
**Copy file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Create file

- **Id:** `onedrive-business/create-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates file.

**Use case.** In work OneDrive files, drop **Create file** on the canvas. Creates file.

**Demonstration.**

```text
**Create file**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Create share link

- **Id:** `onedrive-business/create-share-link`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates share link.

**Use case.** In work OneDrive files, drop **Create share link** on the canvas. Creates share link.

**Demonstration.**

```text
**Create share link**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Create share link by path

- **Id:** `onedrive-business/create-share-link-by-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates share link by path.

**Use case.** In work OneDrive files, drop **Create share link by path** on the canvas. Creates share link by path.

**Demonstration.**

```text
**Create share link by path**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Delete file

- **Id:** `onedrive-business/delete-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes file.

**Use case.** In work OneDrive files, drop **Delete file** on the canvas. Deletes file.

**Demonstration.**

```text
**Delete file**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Extract archive to folder

- **Id:** `onedrive-business/extract-archive-to-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Extracts archive to folder.

**Use case.** In work OneDrive files, drop **Extract archive to folder** on the canvas. Extracts archive to folder.

**Demonstration.**

```text
**Extract archive to folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Find files in folder

- **Id:** `onedrive-business/find-files-in-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Finds files in folder.

**Use case.** In work OneDrive files, drop **Find files in folder** on the canvas. Finds files in folder.

**Demonstration.**

```text
**Find files in folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Find files in folder by path

- **Id:** `onedrive-business/find-files-in-folder-by-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Finds files in folder by path.

**Use case.** In work OneDrive files, drop **Find files in folder by path** on the canvas. Finds files in folder by path.

**Demonstration.**

```text
**Find files in folder by path**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Get file content

- **Id:** `onedrive-business/get-file-content`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file content into a flow variable.

**Use case.** In work OneDrive files, drop **Get file content** on the canvas. Reads file content into a flow variable.

**Demonstration.**

```text
**Get file content**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Get file content using path

- **Id:** `onedrive-business/get-file-content-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file content using path into a flow variable.

**Use case.** In work OneDrive files, drop **Get file content using path** on the canvas. Reads file content using path into a flow variable.

**Demonstration.**

```text
**Get file content using path**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Get file metadata

- **Id:** `onedrive-business/get-file-metadata`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file metadata into a flow variable.

**Use case.** In work OneDrive files, drop **Get file metadata** on the canvas. Reads file metadata into a flow variable.

**Demonstration.**

```text
**Get file metadata**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Get file metadata using path

- **Id:** `onedrive-business/get-file-metadata-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file metadata using path into a flow variable.

**Use case.** In work OneDrive files, drop **Get file metadata using path** on the canvas. Reads file metadata using path into a flow variable.

**Demonstration.**

```text
**Get file metadata using path**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Get file thumbnail

- **Id:** `onedrive-business/get-file-thumbnail`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads file thumbnail into a flow variable.

**Use case.** In work OneDrive files, drop **Get file thumbnail** on the canvas. Reads file thumbnail into a flow variable.

**Demonstration.**

```text
**Get file thumbnail**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### List files in folder

- **Id:** `onedrive-business/list-files-in-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists files in folder.

**Use case.** In work OneDrive files, drop **List files in folder** on the canvas. Lists files in folder.

**Demonstration.**

```text
**List files in folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### List files in root folder

- **Id:** `onedrive-business/list-files-in-root-folder`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists files in root folder.

**Use case.** In work OneDrive files, drop **List files in root folder** on the canvas. Lists files in root folder.

**Demonstration.**

```text
**List files in root folder**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Move or rename a file

- **Id:** `onedrive-business/move-or-rename-a-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Moves or rename a file.

**Use case.** In work OneDrive files, drop **Move or rename a file** on the canvas. Moves or rename a file.

**Demonstration.**

```text
**Move or rename a file**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Move or rename a file using path

- **Id:** `onedrive-business/move-or-rename-a-file-using-path`
- **Kind:** cloud-connector-operation
- **Purpose:** Moves or rename a file using path.

**Use case.** In work OneDrive files, drop **Move or rename a file using path** on the canvas. Moves or rename a file using path.

**Demonstration.**

```text
**Move or rename a file using path**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Update file

- **Id:** `onedrive-business/update-file`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates file.

**Use case.** In work OneDrive files, drop **Update file** on the canvas. Updates file.

**Demonstration.**

```text
**Update file**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.

### Upload file from URL

- **Id:** `onedrive-business/upload-file-from-url`
- **Kind:** cloud-connector-operation
- **Purpose:** Uploads file from URL.

**Use case.** In work OneDrive files, drop **Upload file from URL** on the canvas. Uploads file from URL.

**Demonstration.**

```text
**Upload file from URL**
- (no inputs)
```

**Analogy.** One tool in that kit: your assigned cabinet in the company records room.

**In combination.** Same pattern as OneDrive: content as binary, local convert, then native File/Excel.
