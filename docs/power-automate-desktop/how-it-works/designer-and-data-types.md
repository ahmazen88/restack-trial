# Designer items and data types — how each function works

These are not action cards. They are the workshop (panes, recorders, types) the actions run in.

74 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Access instance

- **Id:** `data-type/access-instance`
- **Kind:** data-type
- **Purpose:** A running Access database from Launch Access.

**Use case.** A variable becomes **Access instance** when PAD infers or you select this shape. A running Access database from Launch Access.

**Demonstration.**

You do not drop **Access instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a running Access database from Launch Access..

**In combination.** Match the type an action produces to the type the next action accepts.

### Action modal

- **Id:** `designer/action-modal`
- **Kind:** usable-item
- **Purpose:** Inputs, produced variables, and On error handling for one action.

**Use case.** Use the **Action modal** surface in the designer or console. Inputs, produced variables, and On error handling for one action.

**Demonstration.**

You do not drop **Action modal** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: inputs, produced variables, and On error handling for one action..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Actions pane

- **Id:** `designer/actions-pane`
- **Kind:** usable-item
- **Purpose:** Left-side catalog of modules and actions, with search and favorites.

**Use case.** Use the **Actions pane** surface in the designer or console. Left-side catalog of modules and actions, with search and favorites.

**Demonstration.**

You do not drop **Actions pane** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: left-side catalog of modules and actions, with search and favorites..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Active Directory entry

- **Id:** `data-type/active-directory-entry`
- **Kind:** data-type
- **Purpose:** AD server connection.

**Use case.** A variable becomes **Active Directory entry** when PAD infers or you select this shape. AD server connection.

**Demonstration.**

You do not drop **Active Directory entry** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — AD server connection..

**In combination.** Match the type an action produces to the type the next action accepts.

### Assets library

- **Id:** `designer/assets-library`
- **Kind:** usable-item
- **Purpose:** Add extra cloud connectors and custom actions to the pane.

**Use case.** Use the **Assets library** surface in the designer or console. Add extra cloud connectors and custom actions to the pane.

**Demonstration.**

You do not drop **Assets library** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: add extra cloud connectors and custom actions to the pane..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Azure client / VM / disk / snapshot / subscription / resource group

- **Id:** `data-type/azure-client-vm-disk-snapshot-subscription-resource-group`
- **Kind:** data-type
- **Purpose:** Azure automation objects.

**Use case.** A variable becomes **Azure client / VM / disk / snapshot / subscription / resource group** when PAD infers or you select this shape. Azure automation objects.

**Demonstration.**

You do not drop **Azure client / VM / disk / snapshot / subscription / resource group** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — azure automation objects..

**In combination.** Match the type an action produces to the type the next action accepts.

### Boolean value

- **Id:** `data-type/boolean-value`
- **Kind:** data-type
- **Purpose:** True or False, often written `%True%` / `%False%`.

**Use case.** A variable becomes **Boolean value** when PAD infers or you select this shape. True or False, often written `%True%` / `%False%`.

**Demonstration.**

You do not drop **Boolean value** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — true or False, often written `%True%` / `%False%`..

**In combination.** Match the type an action produces to the type the next action accepts.

### Breakpoints

- **Id:** `designer/breakpoints`
- **Kind:** usable-item
- **Purpose:** Pause a designer run on a chosen action.

**Use case.** Use the **Breakpoints** surface in the designer or console. Pause a designer run on a chosen action.

**Demonstration.**

You do not drop **Breakpoints** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: pause a designer run on a chosen action..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### CMD session

- **Id:** `data-type/cmd-session`
- **Kind:** data-type
- **Purpose:** Open Command Prompt session.

**Use case.** A variable becomes **CMD session** when PAD infers or you select this shape. Open Command Prompt session.

**Demonstration.**

You do not drop **CMD session** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — open Command Prompt session..

**In combination.** Match the type an action produces to the type the next action accepts.

### Connection references

- **Id:** `designer/connection-references`
- **Kind:** usable-item
- **Purpose:** Cloud connector connections embedded or brought-your-own.

**Use case.** Use the **Connection references** surface in the designer or console. Cloud connector connections embedded or brought-your-own.

**Demonstration.**

You do not drop **Connection references** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: cloud connector connections embedded or brought-your-own..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Connector object

- **Id:** `data-type/connector-object`
- **Kind:** data-type
- **Purpose:** Structured result from a cloud connector operation.

**Use case.** A variable becomes **Connector object** when PAD infers or you select this shape. Structured result from a cloud connector operation.

**Demonstration.**

You do not drop **Connector object** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — structured result from a cloud connector operation..

**In combination.** Match the type an action produces to the type the next action accepts.

### Console - examples

- **Id:** `designer/console-examples`
- **Kind:** usable-item
- **Purpose:** Starter flows shipped with PAD.

**Use case.** Use the **Console - examples** surface in the designer or console. Starter flows shipped with PAD.

**Demonstration.**

You do not drop **Console - examples** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: starter flows shipped with PAD..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Console - My flows

- **Id:** `designer/console-my-flows`
- **Kind:** usable-item
- **Purpose:** Create, run, stop, and schedule desktop flows on this machine.

**Use case.** Use the **Console - My flows** surface in the designer or console. Create, run, stop, and schedule desktop flows on this machine.

**Demonstration.**

You do not drop **Console - My flows** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: create, run, stop, and schedule desktop flows on this machine..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Copilot / natural language

- **Id:** `designer/copilot-natural-language`
- **Kind:** usable-item
- **Purpose:** Describe a task in English to draft actions or Power Fx.

**Use case.** Use the **Copilot / natural language** surface in the designer or console. Describe a task in English to draft actions or Power Fx.

**Demonstration.**

You do not drop **Copilot / natural language** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: describe a task in English to draft actions or Power Fx..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Credential

- **Id:** `data-type/credential`
- **Kind:** data-type
- **Purpose:** Username/password (or similar) from Get credential.

**Use case.** A variable becomes **Credential** when PAD infers or you select this shape. Username/password (or similar) from Get credential.

**Demonstration.**

You do not drop **Credential** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — username/password (or similar) from Get credential..

**In combination.** Match the type an action produces to the type the next action accepts.

### Credentials

- **Id:** `designer/credentials`
- **Kind:** usable-item
- **Purpose:** Portal, Azure Key Vault, or CyberArk secrets resolved at runtime.

**Use case.** Use the **Credentials** surface in the designer or console. Portal, Azure Key Vault, or CyberArk secrets resolved at runtime.

**Demonstration.**

You do not drop **Credentials** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: portal, Azure Key Vault, or CyberArk secrets resolved at runtime..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Custom actions

- **Id:** `designer/custom-actions`
- **Kind:** usable-item
- **Purpose:** Environment-level .dll action groups uploaded by the organization.

**Use case.** Use the **Custom actions** surface in the designer or console. Environment-level .dll action groups uploaded by the organization.

**Demonstration.**

You do not drop **Custom actions** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: environment-level .dll action groups uploaded by the organization..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Custom forms designer

- **Id:** `designer/custom-forms-designer`
- **Kind:** usable-item
- **Purpose:** Layout used by Display custom form.

**Use case.** Use the **Custom forms designer** surface in the designer or console. Layout used by Display custom form.

**Demonstration.**

You do not drop **Custom forms designer** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: layout used by Display custom form..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Custom object

- **Id:** `data-type/custom-object`
- **Kind:** data-type
- **Purpose:** Property/value map, JSON-serializable.

**Use case.** A variable becomes **Custom object** when PAD infers or you select this shape. Property/value map, JSON-serializable.

**Demonstration.**

You do not drop **Custom object** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — property/value map, JSON-serializable..

**In combination.** Match the type an action produces to the type the next action accepts.

### Datarow

- **Id:** `data-type/datarow`
- **Kind:** data-type
- **Purpose:** One row of a datatable, including For each current items.

**Use case.** A variable becomes **Datarow** when PAD infers or you select this shape. One row of a datatable, including For each current items.

**Demonstration.**

You do not drop **Datarow** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — one row of a datatable, including For each current items..

**In combination.** Match the type an action produces to the type the next action accepts.

### Datatable

- **Id:** `data-type/datatable`
- **Kind:** data-type
- **Purpose:** Rows and columns; like a two-dimensional array.

**Use case.** A variable becomes **Datatable** when PAD infers or you select this shape. Rows and columns; like a two-dimensional array.

**Demonstration.**

You do not drop **Datatable** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — rows and columns; like a two-dimensional array..

**In combination.** Match the type an action produces to the type the next action accepts.

### Datetime

- **Id:** `data-type/datetime`
- **Kind:** data-type
- **Purpose:** Date and time, including `%d"yyyy-MM-dd HH:mm:ss"%` literals.

**Use case.** A variable becomes **Datetime** when PAD infers or you select this shape. Date and time, including `%d"yyyy-MM-dd HH:mm:ss"%` literals.

**Demonstration.**

You do not drop **Datetime** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — date and time, including `%d"yyyy-MM-dd HH:mm:ss"%` literals..

**In combination.** Match the type an action produces to the type the next action accepts.

### Desktop recorder

- **Id:** `designer/desktop-recorder`
- **Kind:** usable-item
- **Purpose:** Capture clicks and typing against desktop apps as UI actions.

**Use case.** Use the **Desktop recorder** surface in the designer or console. Capture clicks and typing against desktop apps as UI actions.

**Demonstration.**

You do not drop **Desktop recorder** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: capture clicks and typing against desktop apps as UI actions..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### EC2 client / instance / volume / snapshot

- **Id:** `data-type/ec2-client-instance-volume-snapshot`
- **Kind:** data-type
- **Purpose:** AWS automation objects.

**Use case.** A variable becomes **EC2 client / instance / volume / snapshot** when PAD infers or you select this shape. AWS automation objects.

**Demonstration.**

You do not drop **EC2 client / instance / volume / snapshot** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — AWS automation objects..

**In combination.** Match the type an action produces to the type the next action accepts.

### Environment variables

- **Id:** `designer/environment-variables`
- **Kind:** usable-item
- **Purpose:** Text, number, JSON, boolean, or secret values from the environment.

**Use case.** Use the **Environment variables** surface in the designer or console. Text, number, JSON, boolean, or secret values from the environment.

**Demonstration.**

You do not drop **Environment variables** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: text, number, JSON, boolean, or secret values from the environment..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Error

- **Id:** `data-type/error`
- **Kind:** data-type
- **Purpose:** Last error object from Get last error.

**Use case.** A variable becomes **Error** when PAD infers or you select this shape. Last error object from Get last error.

**Demonstration.**

You do not drop **Error** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — last error object from Get last error..

**In combination.** Match the type an action produces to the type the next action accepts.

### Errors pane

- **Id:** `designer/errors-pane`
- **Kind:** usable-item
- **Purpose:** Design-time and run-time errors and warnings with line and subflow.

**Use case.** Use the **Errors pane** surface in the designer or console. Design-time and run-time errors and warnings with line and subflow.

**Demonstration.**

You do not drop **Errors pane** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: design-time and run-time errors and warnings with line and subflow..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Excel instance

- **Id:** `data-type/excel-instance`
- **Kind:** data-type
- **Purpose:** A running Excel workbook from Launch/Attach Excel.

**Use case.** A variable becomes **Excel instance** when PAD infers or you select this shape. A running Excel workbook from Launch/Attach Excel.

**Demonstration.**

You do not drop **Excel instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a running Excel workbook from Launch/Attach Excel..

**In combination.** Match the type an action produces to the type the next action accepts.

### Exchange connection

- **Id:** `data-type/exchange-connection`
- **Kind:** data-type
- **Purpose:** Open Exchange session.

**Use case.** A variable becomes **Exchange connection** when PAD infers or you select this shape. Open Exchange session.

**Demonstration.**

You do not drop **Exchange connection** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — open Exchange session..

**In combination.** Match the type an action produces to the type the next action accepts.

### Exchange mail message

- **Id:** `data-type/exchange-mail-message`
- **Kind:** data-type
- **Purpose:** Message from Retrieve Exchange email messages.

**Use case.** A variable becomes **Exchange mail message** when PAD infers or you select this shape. Message from Retrieve Exchange email messages.

**Demonstration.**

You do not drop **Exchange mail message** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — message from Retrieve Exchange email messages..

**In combination.** Match the type an action produces to the type the next action accepts.

### File

- **Id:** `data-type/file`
- **Kind:** data-type
- **Purpose:** A file on disk.

**Use case.** A variable becomes **File** when PAD infers or you select this shape. A file on disk.

**Demonstration.**

You do not drop **File** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a file on disk..

**In combination.** Match the type an action produces to the type the next action accepts.

### FileSystemObject

- **Id:** `data-type/filesystemobject`
- **Kind:** data-type
- **Purpose:** Either a file or a folder.

**Use case.** A variable becomes **FileSystemObject** when PAD infers or you select this shape. Either a file or a folder.

**Demonstration.**

You do not drop **FileSystemObject** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — either a file or a folder..

**In combination.** Match the type an action produces to the type the next action accepts.

### Flow designer menus

- **Id:** `designer/flow-designer-menus`
- **Kind:** usable-item
- **Purpose:** Save, save as, undo/redo, find, comments, and enable/disable actions.

**Use case.** Use the **Flow designer menus** surface in the designer or console. Save, save as, undo/redo, find, comments, and enable/disable actions.

**Demonstration.**

You do not drop **Flow designer menus** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: save, save as, undo/redo, find, comments, and enable/disable actions..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Folder

- **Id:** `data-type/folder`
- **Kind:** data-type
- **Purpose:** A folder on disk.

**Use case.** A variable becomes **Folder** when PAD infers or you select this shape. A folder on disk.

**Demonstration.**

You do not drop **Folder** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a folder on disk..

**In combination.** Match the type an action produces to the type the next action accepts.

### FTP connection

- **Id:** `data-type/ftp-connection`
- **Kind:** data-type
- **Purpose:** Open FTP or secure FTP session.

**Use case.** A variable becomes **FTP connection** when PAD infers or you select this shape. Open FTP or secure FTP session.

**Demonstration.**

You do not drop **FTP connection** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — open FTP or secure FTP session..

**In combination.** Match the type an action produces to the type the next action accepts.

### FTP file / FTP directory

- **Id:** `data-type/ftp-file-ftp-directory`
- **Kind:** data-type
- **Purpose:** Remote FTP items.

**Use case.** A variable becomes **FTP file / FTP directory** when PAD infers or you select this shape. Remote FTP items.

**Demonstration.**

You do not drop **FTP file / FTP directory** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — remote FTP items..

**In combination.** Match the type an action produces to the type the next action accepts.

### General value

- **Id:** `data-type/general-value`
- **Kind:** data-type
- **Purpose:** Design-time unknown type; resolved at runtime.

**Use case.** A variable becomes **General value** when PAD infers or you select this shape. Design-time unknown type; resolved at runtime.

**Demonstration.**

You do not drop **General value** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — design-time unknown type; resolved at runtime..

**In combination.** Match the type an action produces to the type the next action accepts.

### Group info / Group member / User info

- **Id:** `data-type/group-info-group-member-user-info`
- **Kind:** data-type
- **Purpose:** AD group and user records.

**Use case.** A variable becomes **Group info / Group member / User info** when PAD infers or you select this shape. AD group and user records.

**Demonstration.**

You do not drop **Group info / Group member / User info** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — AD group and user records..

**In combination.** Match the type an action produces to the type the next action accepts.

### Hosted machines / groups

- **Id:** `designer/hosted-machines-groups`
- **Kind:** usable-item
- **Purpose:** Microsoft-hosted bots for unattended runs.

**Use case.** Use the **Hosted machines / groups** surface in the designer or console. Microsoft-hosted bots for unattended runs.

**Demonstration.**

You do not drop **Hosted machines / groups** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: microsoft-hosted bots for unattended runs..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Images pane

- **Id:** `designer/images-pane`
- **Kind:** usable-item
- **Purpose:** Captured bitmaps used by image-based mouse and wait actions.

**Use case.** Use the **Images pane** surface in the designer or console. Captured bitmaps used by image-based mouse and wait actions.

**Demonstration.**

You do not drop **Images pane** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: captured bitmaps used by image-based mouse and wait actions..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Input / output variables

- **Id:** `designer/input-output-variables`
- **Kind:** usable-item
- **Purpose:** Contract used when a cloud flow or another desktop flow calls this flow.

**Use case.** Use the **Input / output variables** surface in the designer or console. Contract used when a cloud flow or another desktop flow calls this flow.

**Demonstration.**

You do not drop **Input / output variables** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: contract used when a cloud flow or another desktop flow calls this flow..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Keyboard shortcut run

- **Id:** `designer/keyboard-shortcut-run`
- **Kind:** usable-item
- **Purpose:** Start a flow from a hotkey registered in the console.

**Use case.** Use the **Keyboard shortcut run** surface in the designer or console. Start a flow from a hotkey registered in the console.

**Demonstration.**

You do not drop **Keyboard shortcut run** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: start a flow from a hotkey registered in the console..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### List

- **Id:** `data-type/list`
- **Kind:** data-type
- **Purpose:** Zero-based collection of items of a common type.

**Use case.** A variable becomes **List** when PAD infers or you select this shape. Zero-based collection of items of a common type.

**Demonstration.**

You do not drop **List** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — zero-based collection of items of a common type..

**In combination.** Match the type an action produces to the type the next action accepts.

### List of PDF table info

- **Id:** `data-type/list-of-pdf-table-info`
- **Kind:** data-type
- **Purpose:** Tables extracted from a PDF, with page metadata.

**Use case.** A variable becomes **List of PDF table info** when PAD infers or you select this shape. Tables extracted from a PDF, with page metadata.

**Demonstration.**

You do not drop **List of PDF table info** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — tables extracted from a PDF, with page metadata..

**In combination.** Match the type an action produces to the type the next action accepts.

### Machine groups

- **Id:** `designer/machine-groups`
- **Kind:** usable-item
- **Purpose:** Pool machines for unattended scale-out.

**Use case.** Use the **Machine groups** surface in the designer or console. Pool machines for unattended scale-out.

**Demonstration.**

You do not drop **Machine groups** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: pool machines for unattended scale-out..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Machine registration

- **Id:** `designer/machine-registration`
- **Kind:** usable-item
- **Purpose:** Register this PC for attended or unattended cloud-initiated runs.

**Use case.** Use the **Machine registration** surface in the designer or console. Register this PC for attended or unattended cloud-initiated runs.

**Demonstration.**

You do not drop **Machine registration** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: register this PC for attended or unattended cloud-initiated runs..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Mail message

- **Id:** `data-type/mail-message`
- **Kind:** data-type
- **Purpose:** IMAP/SMTP message from Retrieve email messages.

**Use case.** A variable becomes **Mail message** when PAD infers or you select this shape. IMAP/SMTP message from Retrieve email messages.

**Demonstration.**

You do not drop **Mail message** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — IMAP/SMTP message from Retrieve email messages..

**In combination.** Match the type an action produces to the type the next action accepts.

### Numeric value

- **Id:** `data-type/numeric-value`
- **Kind:** data-type
- **Purpose:** A number; the only type allowed in math expressions.

**Use case.** A variable becomes **Numeric value** when PAD infers or you select this shape. A number; the only type allowed in math expressions.

**Demonstration.**

You do not drop **Numeric value** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a number; the only type allowed in math expressions..

**In combination.** Match the type an action produces to the type the next action accepts.

### OCR Engine

- **Id:** `data-type/ocr-engine`
- **Kind:** data-type
- **Purpose:** Windows OCR or Tesseract engine object.

**Use case.** A variable becomes **OCR Engine** when PAD infers or you select this shape. Windows OCR or Tesseract engine object.

**Demonstration.**

You do not drop **OCR Engine** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — windows OCR or Tesseract engine object..

**In combination.** Match the type an action produces to the type the next action accepts.

### On error handling

- **Id:** `designer/on-error-handling`
- **Kind:** usable-item
- **Purpose:** Retry, continue, throw, or go-to-label when an action fails.

**Use case.** Use the **On error handling** surface in the designer or console. Retry, continue, throw, or go-to-label when an action fails.

**Demonstration.**

You do not drop **On error handling** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: retry, continue, throw, or go-to-label when an action fails..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Outlook instance

- **Id:** `data-type/outlook-instance`
- **Kind:** data-type
- **Purpose:** A running Outlook profile from Launch Outlook.

**Use case.** A variable becomes **Outlook instance** when PAD infers or you select this shape. A running Outlook profile from Launch Outlook.

**Demonstration.**

You do not drop **Outlook instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a running Outlook profile from Launch Outlook..

**In combination.** Match the type an action produces to the type the next action accepts.

### Outlook mail message

- **Id:** `data-type/outlook-mail-message`
- **Kind:** data-type
- **Purpose:** Message from Retrieve email messages from Outlook.

**Use case.** A variable becomes **Outlook mail message** when PAD infers or you select this shape. Message from Retrieve email messages from Outlook.

**Demonstration.**

You do not drop **Outlook mail message** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — message from Retrieve email messages from Outlook..

**In combination.** Match the type an action produces to the type the next action accepts.

### Power Fx toggle

- **Id:** `designer/power-fx-toggle`
- **Kind:** usable-item
- **Purpose:** Per-flow choice of classic % expressions versus Power Fx.

**Use case.** Use the **Power Fx toggle** surface in the designer or console. Per-flow choice of classic % expressions versus Power Fx.

**Demonstration.**

You do not drop **Power Fx toggle** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: per-flow choice of classic % expressions versus Power Fx..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Run / Run from here / Step

- **Id:** `designer/run-run-from-here-step`
- **Kind:** usable-item
- **Purpose:** Designer debug controls for the current flow.

**Use case.** Use the **Run / Run from here / Step** surface in the designer or console. Designer debug controls for the current flow.

**Demonstration.**

You do not drop **Run / Run from here / Step** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: designer debug controls for the current flow..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Safe stop

- **Id:** `designer/safe-stop`
- **Kind:** usable-item
- **Purpose:** Cooperative stop check via If safe stop requested.

**Use case.** Use the **Safe stop** surface in the designer or console. Cooperative stop check via If safe stop requested.

**Demonstration.**

You do not drop **Safe stop** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: cooperative stop check via If safe stop requested..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Selectors

- **Id:** `designer/selectors`
- **Kind:** usable-item
- **Purpose:** Text-based (and repaired) locators behind UI and web elements.

**Use case.** Use the **Selectors** surface in the designer or console. Text-based (and repaired) locators behind UI and web elements.

**Demonstration.**

You do not drop **Selectors** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: text-based (and repaired) locators behind UI and web elements..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Sensitive variables

- **Id:** `designer/sensitive-variables`
- **Kind:** usable-item
- **Purpose:** Mask values in logs and the variables viewer.

**Use case.** Use the **Sensitive variables** surface in the designer or console. Mask values in logs and the variables viewer.

**Demonstration.**

You do not drop **Sensitive variables** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: mask values in logs and the variables viewer..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### SensitiveValue

- **Id:** `data-type/sensitivevalue`
- **Kind:** data-type
- **Purpose:** Masked text such as passwords and secret environment variables.

**Use case.** A variable becomes **SensitiveValue** when PAD infers or you select this shape. Masked text such as passwords and secret environment variables.

**Demonstration.**

You do not drop **SensitiveValue** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — masked text such as passwords and secret environment variables..

**In combination.** Match the type an action produces to the type the next action accepts.

### SQL connection

- **Id:** `data-type/sql-connection`
- **Kind:** data-type
- **Purpose:** Open database session.

**Use case.** A variable becomes **SQL connection** when PAD infers or you select this shape. Open database session.

**Demonstration.**

You do not drop **SQL connection** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — open database session..

**In combination.** Match the type an action produces to the type the next action accepts.

### Subflows

- **Id:** `designer/subflows`
- **Kind:** usable-item
- **Purpose:** Tabs besides Main; invoke with Run subflow, including dynamic names.

**Use case.** Use the **Subflows** surface in the designer or console. Tabs besides Main; invoke with Run subflow, including dynamic names.

**Demonstration.**

You do not drop **Subflows** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: tabs besides Main; invoke with Run subflow, including dynamic names..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Terminal session

- **Id:** `data-type/terminal-session`
- **Kind:** data-type
- **Purpose:** Open terminal emulator session.

**Use case.** A variable becomes **Terminal session** when PAD infers or you select this shape. Open terminal emulator session.

**Demonstration.**

You do not drop **Terminal session** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — open terminal emulator session..

**In combination.** Match the type an action produces to the type the next action accepts.

### Text value

- **Id:** `data-type/text-value`
- **Kind:** data-type
- **Purpose:** Any string, including paths and file contents.

**Use case.** A variable becomes **Text value** when PAD infers or you select this shape. Any string, including paths and file contents.

**Demonstration.**

You do not drop **Text value** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — any string, including paths and file contents..

**In combination.** Match the type an action produces to the type the next action accepts.

### TriggerEventInstanceHandle

- **Id:** `data-type/triggereventinstancehandle`
- **Kind:** data-type
- **Purpose:** Handle raised by a UI element event trigger.

**Use case.** A variable becomes **TriggerEventInstanceHandle** when PAD infers or you select this shape. Handle raised by a UI element event trigger.

**Demonstration.**

You do not drop **TriggerEventInstanceHandle** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — handle raised by a UI element event trigger..

**In combination.** Match the type an action produces to the type the next action accepts.

### UI element collections

- **Id:** `designer/ui-element-collections`
- **Kind:** usable-item
- **Purpose:** Reusable shared UI element sets across flows.

**Use case.** Use the **UI element collections** surface in the designer or console. Reusable shared UI element sets across flows.

**Demonstration.**

You do not drop **UI element collections** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: reusable shared UI element sets across flows..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### UI elements pane

- **Id:** `designer/ui-elements-pane`
- **Kind:** usable-item
- **Purpose:** Captured desktop and web elements plus their selectors.

**Use case.** Use the **UI elements pane** surface in the designer or console. Captured desktop and web elements plus their selectors.

**Demonstration.**

You do not drop **UI elements pane** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: captured desktop and web elements plus their selectors..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### URL / scheme run

- **Id:** `designer/url-scheme-run`
- **Kind:** usable-item
- **Purpose:** Start a flow from a PAD URL shortcut.

**Use case.** Use the **URL / scheme run** surface in the designer or console. Start a flow from a PAD URL shortcut.

**Demonstration.**

You do not drop **URL / scheme run** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: start a flow from a PAD URL shortcut..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Variables pane

- **Id:** `designer/variables-pane`
- **Kind:** usable-item
- **Purpose:** Input, output, and flow variables, including sensitive and optional flags.

**Use case.** Use the **Variables pane** surface in the designer or console. Input, output, and flow variables, including sensitive and optional flags.

**Demonstration.**

You do not drop **Variables pane** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: input, output, and flow variables, including sensitive and optional flags..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Web browser instance

- **Id:** `data-type/web-browser-instance`
- **Kind:** data-type
- **Purpose:** Chrome, Edge, Firefox, or IE session.

**Use case.** A variable becomes **Web browser instance** when PAD infers or you select this shape. Chrome, Edge, Firefox, or IE session.

**Demonstration.**

You do not drop **Web browser instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — chrome, Edge, Firefox, or IE session..

**In combination.** Match the type an action produces to the type the next action accepts.

### Web recorder

- **Id:** `designer/web-recorder`
- **Kind:** usable-item
- **Purpose:** Capture browser interactions as web automation actions.

**Use case.** Use the **Web recorder** surface in the designer or console. Capture browser interactions as web automation actions.

**Demonstration.**

You do not drop **Web recorder** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: capture browser interactions as web automation actions..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Window instance

- **Id:** `data-type/window-instance`
- **Kind:** data-type
- **Purpose:** A desktop window from Get window.

**Use case.** A variable becomes **Window instance** when PAD infers or you select this shape. A desktop window from Get window.

**Demonstration.**

You do not drop **Window instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a desktop window from Get window..

**In combination.** Match the type an action produces to the type the next action accepts.

### Word instance

- **Id:** `data-type/word-instance`
- **Kind:** data-type
- **Purpose:** A running Word document from Launch/Attach Word.

**Use case.** A variable becomes **Word instance** when PAD infers or you select this shape. A running Word document from Launch/Attach Word.

**Demonstration.**

You do not drop **Word instance** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — a running Word document from Launch/Attach Word..

**In combination.** Match the type an action produces to the type the next action accepts.

### Work queues

- **Id:** `designer/work-queues`
- **Kind:** usable-item
- **Purpose:** Orchestrated item processing across unattended machines.

**Use case.** Use the **Work queues** surface in the designer or console. Orchestrated item processing across unattended machines.

**Demonstration.**

You do not drop **Work queues** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: orchestrated item processing across unattended machines..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### Workspace canvas

- **Id:** `designer/workspace-canvas`
- **Kind:** usable-item
- **Purpose:** Ordered list of deployed actions for the active subflow.

**Use case.** Use the **Workspace canvas** surface in the designer or console. Ordered list of deployed actions for the active subflow.

**Demonstration.**

You do not drop **Workspace canvas** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** A part of the workshop itself, not a tool you pick up: ordered list of deployed actions for the active subflow..

**In combination.** Use these surfaces while you place the actions in the combination playbooks.

### XML node

- **Id:** `data-type/xml-node`
- **Kind:** data-type
- **Purpose:** Loaded XML document or fragment.

**Use case.** A variable becomes **XML node** when PAD infers or you select this shape. Loaded XML document or fragment.

**Demonstration.**

You do not drop **XML node** as an action card. You use it in the designer while building other actions (pane, type of a variable, or a locator).

**Analogy.** The shape of the thing in the jar — loaded XML document or fragment..

**In combination.** Match the type an action produces to the type the next action accepts.
