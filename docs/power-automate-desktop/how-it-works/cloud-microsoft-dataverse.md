# Microsoft Dataverse (cloud) — how each function works

Default-pane **Microsoft Dataverse (cloud)** operations. Requires a connection reference. File payloads are binary data.

11 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Add a new row to selected environment

- **Id:** `microsoft-dataverse/add-a-new-row-to-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Adds a new row to selected environment.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Add a new row to selected environment** on the canvas. Adds a new row to selected environment.

**Demonstration.**

```text
**Add a new row to selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Delete a row from selected environment

- **Id:** `microsoft-dataverse/delete-a-row-from-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes a row from selected environment.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Delete a row from selected environment** on the canvas. Deletes a row from selected environment.

**Demonstration.**

```text
**Delete a row from selected environment**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Download a file or an image from selected environment

- **Id:** `microsoft-dataverse/download-a-file-or-an-image-from-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Downloads a file or an image from selected environment.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Download a file or an image from selected environment** on the canvas. Downloads a file or an image from selected environment.

**Demonstration.**

```text
**Download a file or an image from selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Get a row by ID from selected environment

- **Id:** `microsoft-dataverse/get-a-row-by-id-from-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads a row by ID from selected environment into a flow variable.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Get a row by ID from selected environment** on the canvas. Reads a row by ID from selected environment into a flow variable.

**Demonstration.**

```text
**Get a row by ID from selected environment**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### List rows from selected environment

- **Id:** `microsoft-dataverse/list-rows-from-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Lists rows from selected environment.

**Use case.** In rows in Dataverse the cloud team already uses, drop **List rows from selected environment** on the canvas. Lists rows from selected environment.

**Demonstration.**

```text
**List rows from selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Perform a bound action in selected environment

- **Id:** `microsoft-dataverse/perform-a-bound-action-in-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Perform a bound action in selected environment** from the actions pane.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Perform a bound action in selected environment** on the canvas. Runs **Perform a bound action in selected environment** from the actions pane.

**Demonstration.**

```text
**Perform a bound action in selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Perform an unbound action in selected environment

- **Id:** `microsoft-dataverse/perform-an-unbound-action-in-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Perform an unbound action in selected environment** from the actions pane.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Perform an unbound action in selected environment** on the canvas. Runs **Perform an unbound action in selected environment** from the actions pane.

**Demonstration.**

```text
**Perform an unbound action in selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Relate rows in selected environment

- **Id:** `microsoft-dataverse/relate-rows-in-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Relate rows in selected environment** from the actions pane.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Relate rows in selected environment** on the canvas. Runs **Relate rows in selected environment** from the actions pane.

**Demonstration.**

```text
**Relate rows in selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Unrelate rows in selected environment

- **Id:** `microsoft-dataverse/unrelate-rows-in-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Unrelate rows in selected environment** from the actions pane.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Unrelate rows in selected environment** on the canvas. Runs **Unrelate rows in selected environment** from the actions pane.

**Demonstration.**

```text
**Unrelate rows in selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Upload a file or an image to selected environment

- **Id:** `microsoft-dataverse/upload-a-file-or-an-image-to-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Uploads a file or an image to selected environment.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Upload a file or an image to selected environment** on the canvas. Uploads a file or an image to selected environment.

**Demonstration.**

```text
**Upload a file or an image to selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.

### Upsert a row in selected environment

- **Id:** `microsoft-dataverse/upsert-a-row-in-selected-environment`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Upsert a row in selected environment** from the actions pane.

**Use case.** In rows in Dataverse the cloud team already uses, drop **Upsert a row in selected environment** on the canvas. Runs **Upsert a row in selected environment** from the actions pane.

**Demonstration.**

```text
**Upsert a row in selected environment**
- (no inputs)
```

**Analogy.** One tool in that kit: the official company register instead of a side spreadsheet.

**In combination.** List rows or Get a row by ID, then Update/Add a row in the same environment.
