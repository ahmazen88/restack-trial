# OneNote (cloud) — how each function works

Default-pane **OneNote (cloud)** operations. Requires a connection reference. File payloads are binary data.

9 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Create a page in Quick Notes

- **Id:** `onenote/create-a-page-in-quick-notes`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates a page in Quick Notes.

**Use case.** In run notes a human reads later, drop **Create a page in Quick Notes** on the canvas. Creates a page in Quick Notes.

**Demonstration.**

```text
**Create a page in Quick Notes**
- (no inputs)
```

**Analogy.** One tool in that kit: a lab notebook on the shelf.

**In combination.** Create page in a section with the run summary after Log message.

### Create page in a section

- **Id:** `onenote/create-page-in-a-section`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates page in a section.

**Use case.** In run notes a human reads later, drop **Create page in a section** on the canvas. Creates page in a section.

**Demonstration.**

```text
**Create page in a section**
- (no inputs)
```

**Analogy.** One tool in that kit: a lab notebook on the shelf.

**In combination.** Create page in a section with the run summary after Log message.

### Create section in a notebook

- **Id:** `onenote/create-section-in-a-notebook`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates section in a notebook.

**Use case.** In run notes a human reads later, drop **Create section in a notebook** on the canvas. Creates section in a notebook.

**Demonstration.**

```text
**Create section in a notebook**
- (no inputs)
```

**Analogy.** One tool in that kit: a lab notebook on the shelf.

**In combination.** Create page in a section with the run summary after Log message.

### Delete a page

- **Id:** `onenote/delete-a-page`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes a page.

**Use case.** In run notes a human reads later, drop **Delete a page** on the canvas. Deletes a page.

**Demonstration.**

```text
**Delete a page**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Create page in a section with the run summary after Log message.

### Get page content

- **Id:** `onenote/get-page-content`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads page content into a flow variable.

**Use case.** In run notes a human reads later, drop **Get page content** on the canvas. Reads page content into a flow variable.

**Demonstration.**

```text
**Get page content**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Create page in a section with the run summary after Log message.

### Get pages for a specific section

- **Id:** `onenote/get-pages-for-a-specific-section`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads pages for a specific section into a flow variable.

**Use case.** In run notes a human reads later, drop **Get pages for a specific section** on the canvas. Reads pages for a specific section into a flow variable.

**Demonstration.**

```text
**Get pages for a specific section**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Create page in a section with the run summary after Log message.

### Get recent notebooks

- **Id:** `onenote/get-recent-notebooks`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads recent notebooks into a flow variable.

**Use case.** In run notes a human reads later, drop **Get recent notebooks** on the canvas. Reads recent notebooks into a flow variable.

**Demonstration.**

```text
**Get recent notebooks**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Create page in a section with the run summary after Log message.

### Get sections in notebook

- **Id:** `onenote/get-sections-in-notebook`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads sections in notebook into a flow variable.

**Use case.** In run notes a human reads later, drop **Get sections in notebook** on the canvas. Reads sections in notebook into a flow variable.

**Demonstration.**

```text
**Get sections in notebook**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Create page in a section with the run summary after Log message.

### Update page content

- **Id:** `onenote/update-page-content`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates page content.

**Use case.** In run notes a human reads later, drop **Update page content** on the canvas. Updates page content.

**Demonstration.**

```text
**Update page content**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Create page in a section with the run summary after Log message.
