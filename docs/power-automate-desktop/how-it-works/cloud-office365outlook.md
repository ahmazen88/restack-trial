# Office 365 Outlook (cloud) — how each function works

Default-pane **Office 365 Outlook (cloud)** operations. Requires a connection reference. File payloads are binary data.

31 items. Each entry has a use case, a designer-style demonstration, an analogy, and how it combines with neighbors.

### Create contact (V2)

- **Id:** `office365outlook/create-contact-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates contact (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Create contact (V2)** on the canvas. Creates contact (V2).

**Demonstration.**

```text
**Create contact (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Create event (V4)

- **Id:** `office365outlook/create-event-v4`
- **Kind:** cloud-connector-operation
- **Purpose:** Creates event (V4).

**Use case.** In mailbox work without a local Outlook install, drop **Create event (V4)** on the canvas. Creates event (V4).

**Demonstration.**

```text
**Create event (V4)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Delete contact (V2)

- **Id:** `office365outlook/delete-contact-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes contact (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Delete contact (V2)** on the canvas. Deletes contact (V2).

**Demonstration.**

```text
**Delete contact (V2)**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Delete email (V2)

- **Id:** `office365outlook/delete-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes email (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Delete email (V2)** on the canvas. Deletes email (V2).

**Demonstration.**

```text
**Delete email (V2)**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Delete event (V2)

- **Id:** `office365outlook/delete-event-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Deletes event (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Delete event (V2)** on the canvas. Deletes event (V2).

**Demonstration.**

```text
**Delete event (V2)**
- (no inputs)
```

**Analogy.** Taking a page out of the folder so it is gone.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Export email (V2)

- **Id:** `office365outlook/export-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Export email (V2)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Export email (V2)** on the canvas. Runs **Export email (V2)** from the actions pane.

**Demonstration.**

```text
**Export email (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Find meeting times (V2)

- **Id:** `office365outlook/find-meeting-times-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Finds meeting times (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Find meeting times (V2)** on the canvas. Finds meeting times (V2).

**Demonstration.**

```text
**Find meeting times (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Flag email (V2)

- **Id:** `office365outlook/flag-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Flag email (V2)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Flag email (V2)** on the canvas. Runs **Flag email (V2)** from the actions pane.

**Demonstration.**

```text
**Flag email (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Forward an email (V2)

- **Id:** `office365outlook/forward-an-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Forward an email (V2)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Forward an email (V2)** on the canvas. Runs **Forward an email (V2)** from the actions pane.

**Demonstration.**

```text
**Forward an email (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get Attachment (V2)

- **Id:** `office365outlook/get-attachment-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads attachment (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get Attachment (V2)** on the canvas. Reads attachment (V2) into a flow variable.

**Demonstration.**

```text
**Get Attachment (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get calendar view of events (V3)

- **Id:** `office365outlook/get-calendar-view-of-events-v3`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads calendar view of events (V3) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get calendar view of events (V3)** on the canvas. Reads calendar view of events (V3) into a flow variable.

**Demonstration.**

```text
**Get calendar view of events (V3)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get calendars (V2)

- **Id:** `office365outlook/get-calendars-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads calendars (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get calendars (V2)** on the canvas. Reads calendars (V2) into a flow variable.

**Demonstration.**

```text
**Get calendars (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get contact (V2)

- **Id:** `office365outlook/get-contact-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads contact (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get contact (V2)** on the canvas. Reads contact (V2) into a flow variable.

**Demonstration.**

```text
**Get contact (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get contacts (V2)

- **Id:** `office365outlook/get-contacts-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads contacts (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get contacts (V2)** on the canvas. Reads contacts (V2) into a flow variable.

**Demonstration.**

```text
**Get contacts (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get email (V2)

- **Id:** `office365outlook/get-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads email (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get email (V2)** on the canvas. Reads email (V2) into a flow variable.

**Demonstration.**

```text
**Get email (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get emails (V3)

- **Id:** `office365outlook/get-emails-v3`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads emails (V3) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get emails (V3)** on the canvas. Reads emails (V3) into a flow variable.

**Demonstration.**

```text
**Get emails (V3)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get event (V3)

- **Id:** `office365outlook/get-event-v3`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads event (V3) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get event (V3)** on the canvas. Reads event (V3) into a flow variable.

**Demonstration.**

```text
**Get event (V3)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get events (V4)

- **Id:** `office365outlook/get-events-v4`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads events (V4) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get events (V4)** on the canvas. Reads events (V4) into a flow variable.

**Demonstration.**

```text
**Get events (V4)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get room lists (V2)

- **Id:** `office365outlook/get-room-lists-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads room lists (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get room lists (V2)** on the canvas. Reads room lists (V2) into a flow variable.

**Demonstration.**

```text
**Get room lists (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get rooms (V2)

- **Id:** `office365outlook/get-rooms-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads rooms (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get rooms (V2)** on the canvas. Reads rooms (V2) into a flow variable.

**Demonstration.**

```text
**Get rooms (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Get rooms in room list (V2)

- **Id:** `office365outlook/get-rooms-in-room-list-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Reads rooms in room list (V2) into a flow variable.

**Use case.** In mailbox work without a local Outlook install, drop **Get rooms in room list (V2)** on the canvas. Reads rooms in room list (V2) into a flow variable.

**Demonstration.**

```text
**Get rooms in room list (V2)**
- (no inputs)
```

**Analogy.** Copying a value off a page into your notebook.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Mark as read or unread (V3)

- **Id:** `office365outlook/mark-as-read-or-unread-v3`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Mark as read or unread (V3)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Mark as read or unread (V3)** on the canvas. Runs **Mark as read or unread (V3)** from the actions pane.

**Demonstration.**

```text
**Mark as read or unread (V3)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Move email (V2)

- **Id:** `office365outlook/move-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Moves email (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Move email (V2)** on the canvas. Moves email (V2).

**Demonstration.**

```text
**Move email (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Reply to email (V3)

- **Id:** `office365outlook/reply-to-email-v3`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Reply to email (V3)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Reply to email (V3)** on the canvas. Runs **Reply to email (V3)** from the actions pane.

**Demonstration.**

```text
**Reply to email (V3)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Respond to an event invite (V2)

- **Id:** `office365outlook/respond-to-an-event-invite-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Runs **Respond to an event invite (V2)** from the actions pane.

**Use case.** In mailbox work without a local Outlook install, drop **Respond to an event invite (V2)** on the canvas. Runs **Respond to an event invite (V2)** from the actions pane.

**Demonstration.**

```text
**Respond to an event invite (V2)**
- (no inputs)
```

**Analogy.** One tool in that kit: webmail at the counter instead of the tray on your desk.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Send an email (V2)

- **Id:** `office365outlook/send-an-email-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Sends an email (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Send an email (V2)** on the canvas. Sends an email (V2).

**Demonstration.**

```text
**Send an email (V2)**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Send an email from a shared mailbox (V2)

- **Id:** `office365outlook/send-an-email-from-a-shared-mailbox-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Sends an email from a shared mailbox (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Send an email from a shared mailbox (V2)** on the canvas. Sends an email from a shared mailbox (V2).

**Demonstration.**

```text
**Send an email from a shared mailbox (V2)**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Send an HTTP request

- **Id:** `office365outlook/send-an-http-request`
- **Kind:** cloud-connector-operation
- **Purpose:** Sends an HTTP request.

**Use case.** In mailbox work without a local Outlook install, drop **Send an HTTP request** on the canvas. Sends an HTTP request.

**Demonstration.**

```text
**Send an HTTP request**
- (no inputs)
```

**Analogy.** Handing a finished envelope to the mailroom.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Set up automatic replies (V2)

- **Id:** `office365outlook/set-up-automatic-replies-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Writes up automatic replies (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Set up automatic replies (V2)** on the canvas. Writes up automatic replies (V2).

**Demonstration.**

```text
**Set up automatic replies (V2)**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Update contact (V2)

- **Id:** `office365outlook/update-contact-v2`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates contact (V2).

**Use case.** In mailbox work without a local Outlook install, drop **Update contact (V2)** on the canvas. Updates contact (V2).

**Demonstration.**

```text
**Update contact (V2)**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Convert file to binary data before Send an email (V2) attachments.

### Update event (V4)

- **Id:** `office365outlook/update-event-v4`
- **Kind:** cloud-connector-operation
- **Purpose:** Updates event (V4).

**Use case.** In mailbox work without a local Outlook install, drop **Update event (V4)** on the canvas. Updates event (V4).

**Demonstration.**

```text
**Update event (V4)**
- (no inputs)
```

**Analogy.** Writing a value onto a page so the next person (or action) can see it.

**In combination.** Convert file to binary data before Send an email (V2) attachments.
