# Cloud connectors

Run Power Automate cloud connector operations inside a desktop flow. Default connectors are always visible. Add more from the Assets library. Pass files as binary data (`Convert file to binary data` / `Convert binary data to file`).

This page documents every **native action** in this group (0 items).

## Actions
