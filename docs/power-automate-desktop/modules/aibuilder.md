# AI Builder (preview)

Call GPT text generation from a desktop flow. Human review is required.

This page documents every **native action** in this group (1 items).

## Actions

### Create text with GPT (preview)

- **Inventory id:** `aibuilder/create-text-with-gpt-preview`
- **Kind:** native-action
- **Purpose:** Creates text with GPT (preview).
- **Key inputs:** `Instructions` (Text value)
- **Produces:** `PredictV2Response` (Connector object); `PredictV2TextResponse` (Text)
- **Exceptions:** `Endpoint failure`
- **Microsoft Learn:** [Create text with GPT (preview)](https://learn.microsoft.com/en-us/power-automate/desktop-flows/actions-reference/aibuilder#callgpt)
